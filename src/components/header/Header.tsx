import React, { FC } from 'react';
import BreadCrumbs from '../breadcrumbs/BreadCrumbs';

const Header: FC = () => {
  return (
    <header>
      <BreadCrumbs />
    </header>
  );
};

export default Header;
